{
  "name": "tiny-market",
  "version": "1.0.0",
  "description": "A tiny marketplace smart contract for the Stacks blockchain",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@hirosystems/clarinet-sdk": "^2.8.0",
    "vite": "^5.4.8",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "vitest-environment-clarinet": "^2.3.0"
  }
}
